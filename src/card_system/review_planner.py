"""复习计划生成业务骨架。"""

from schema.review import ReviewPlan


async def generate_review_plan(weak_points: list[str]) -> ReviewPlan:
    """根据薄弱点生成复习计划，具体规划逻辑在后续阶段实现。"""
    raise NotImplementedError("Review planning will be implemented in a later stage.")

